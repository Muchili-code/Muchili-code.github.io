# GitHub的奇怪指南

## 远程仓库

LH同学在本地构建了仓库并链接到自己的GitHub账号仓库`srtp-listenning`，同时将GitHub仓库的共同处理权限给我的GitHub账号。LH同学将LH本地的文件传到远程仓库的`main`分支

我在本地构建了仓库，链接了LH同学的远程GitHub账号，并将代码传到另外一个分支`master`。

我在GitHub desktop导入本地仓库，git自动识别到远程仓库`srtpxxxxx`。此时识别到我的分支是`master`。我切换到`main`分支，<span style="background:#99FF99;">本地文件自动切换到`main`分支的文件</span>。